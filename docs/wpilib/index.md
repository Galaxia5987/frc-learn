# Introduction to WPILib

The WPI Robotics Library(WPILib) is the standard software library provided for teams to write code for FRC robots. WPILib contains a set of useful classes and functions for interfacing with various parts of the FRC control system(such as sensors, motor controllers, and the driver station), as well as other utility functions.

In order to write code for our robot, we must use WPILib tools and functions to control everything.
WPILib provides the way to control our motors.

WPILib is built for Java, but we can also use Kotlin. As Java and Kotlin are fully compatible, we can also write our code in Kotlin.

## How will we learn

As WPILib has great docs, some of the topics will be taken straight from the docs with minor modifications. The problem with WPILib docs is that it is very messy, has unrelated, or obsolete topics that can be confusing. This guide aims to make learning it better. The official docs are still much more comprehensive.

Some of the topics will have original writing. All the topics taken from other sites will have appropriate attribution using a note at the top of the page.